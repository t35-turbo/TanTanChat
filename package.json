{
  "scripts": {
    "dev": "dotenv -- concurrently -n 'client,server' -c 'blue,magenta' 'pnpm -C client dev' 'pnpm -C server dev'",
    "db:push": "dotenv -- pnpm -C server db:push",
    "db:studio": "dotenv -- pnpm -C server db:studio",
    "typecheck": "pnpm -C client typecheck && pnpm -C server typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "dotenv-cli": "^10.0.0",
    "prettier": "^3.6.2",
    "prettier-plugin-organize-imports": "^4.2.0",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "TanTanChat": "link:"
  }
}